'use strict'

exports.FieldType = {
    INTEGER: 'dont care',
    FLOAT: 'dont care'
}
exports.InfluxDB = function () {}
exports.InfluxDB.prototype.writePoints = (points) => {
    console.log(JSON.stringify(points[0]))

    return {
        then(call) {
            call()
            return {
                catch() {
                }
            }
        }
    }
}